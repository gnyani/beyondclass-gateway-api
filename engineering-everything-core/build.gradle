
apply plugin: 'groovy'
apply plugin: 'java'
repositories {
    mavenCentral()
}

dependencies {
    compile(libs.groovy)
    compile(libs.springBootStarterWeb)
    testCompile(libs.springBootStarterTest)
    testCompile (libs.junit)
}
